{
  "name": "posthog-watcher-action",
  "version": "0.1.0",
  "private": true,
  "description": "A GitHub Action that uses pi to triage issues and optionally open straightforward fix PRs.",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "build": "tsc --noEmit && node scripts/build.mjs",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "esbuild": "^0.27.1",
    "typescript": "^6.0.3"
  }
}
